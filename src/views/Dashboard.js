import React, { Fragment } from "react";

import {DisplayMapClass} from '../utils/maps';

const Dashboard = () => (
  <Fragment>
            <DisplayMapClass />
  </Fragment>
);

export default Dashboard;
